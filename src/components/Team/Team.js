import React from "react";
import "./Team.css";

import React from "react";

export default function Team() {
  return (
    <div className="Team-card">
      <p>{name}</p>
      <p>{points} points</p>
      <button>x</button>
    </div>
  );
}
